# PR CI Validation

This file is used to validate the GitHub Actions pull request workflow.

Expected behavior:
- Opening a pull request to `main` triggers the `CI` workflow.
- The CI workflow must pass before the pull request is merged.
